# Project 2 — Log Analyzer

Build a CLI that reads a text log and reports counts for INFO, WARNING and ERROR lines.

Requirements:
- Read a file safely.
- Count levels.
- Export JSON.
- Handle a missing file.
- Use argparse and logging.
- Add tests.

Extension: detect repeated error messages and output a CSV report.
