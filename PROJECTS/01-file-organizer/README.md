# Project 1 — File Organizer

Build a CLI that scans a directory and groups files by extension.

Requirements:
- Use pathlib.
- Do not delete files automatically.
- Support a dry-run preview.
- Report how many files were found per extension.
- Handle missing directories.
- Add tests for classification logic.

Suggested commands:
`python organizer.py --path ./downloads --dry-run`
